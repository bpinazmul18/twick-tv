// ActionTypes

// Actions

// Initial state
const initalState = {
  loading: false,
  list: [],
  lastFetch: null,
}

// Reducer
const streamsReducer = (streams = initalState, action) => {
  return streams
}

export default streamsReducer
